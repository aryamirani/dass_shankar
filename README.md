# Health Problems — Accessible Lessons

Prototype demonstrating an accessible, multisensory lesson for children with special needs.

- To run locally:

```bash
# install deps
npm install
# start dev server
npm run dev
# open the URL shown by vite (e.g. http://localhost:5173)
```

Notes:
- The landing page uses a circle of 12 large PNGs. Click any image to open its lesson.
